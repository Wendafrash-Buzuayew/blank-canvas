# Local Development Overrides
# Copy this file to .env.local and adjust as needed

# When running through Vite dev server, use /api with proxy.
# If connecting directly to the gateway without proxy, uncomment the line below.
# VITE_API_BASE_URL=http://localhost:8081/api